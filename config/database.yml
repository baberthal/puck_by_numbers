default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5
  username: rails
  password: ilikeruby
  host: 10.1.10.2
  timeout: 5000

development:
  <<: *default
  database: puck_by_numbers_dev

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: puck_by_numbers_test

production:
  <<: *default
  database: puck_by_numbers
